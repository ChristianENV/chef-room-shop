/** HttpOnly cookie name for anonymous guest checkout / design sessions. */
export const GUEST_SESSION_COOKIE_NAME = 'chefroom_guest'

/** Guest session lifetime in days (cookie + DB `expiresAt`). */
export const GUEST_SESSION_TTL_DAYS = 30
